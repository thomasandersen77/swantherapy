services:
  - type: web
    name: swantherapy
    runtime: static
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: ./build
    envVars:
      - key: VITE_API_BASE_URL
        value: https://swantherapy.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
